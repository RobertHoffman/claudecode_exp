# Worker 审计 Prompt — 直接可用版

Driver 执行以下步骤：

```bash
# 1. 收集 git diff（如有版本管理）
GIT_DIFF=$(cd /home/rucli/stock && git diff HEAD~1 --stat 2>/dev/null || echo "无 git diff")

# 2. 收集相关源文件内容（修改过的文件）
FILE_CONTENT=$(cat /home/rucli/stock/相关文件路径)

# 3. 将审计文件内容注入下面 [Context] 并发送
ccs minimax-worker "prompt"
```

---

[Context]

项目路径: /home/rucli/stock/
项目概述: A 股事件驱动选股平台。data_layer / factor_layer / event_detection 为核心调用链。

变更范围:
- 改动文件: {手动填入文件列表，如 core/storage.py data_layer/fetcher.py}
- git diff stat: {手动填入 git diff --stat 输出}

修改后源文件内容:
```python
# {文件路径}
{手动填入文件完整内容}
```

```python
# {下一个文件路径}
{手动填入文件完整内容}
```

[Task]

你作为代码审计专家，对以上变更执行 4 步审计：

## 第 1 步：静态检查
检查 ruff 规则违规：BLE001（盲捕异常）、DTZ005/007/011（时区）、F821（未定义变量）、E501（超长行）、SIM105（suppress 替换）。直接输出发现的违规。

## 第 2 步：执行测试
- 运行 `cd /home/rucli/stock && python3 -m pytest tests/相关测试文件 -v`（路径可能不存在则跳过）
- 运行 `cd /home/rucli/stock && python3 -c "from {修改模块} import {相关对象}; print('OK')"`
- 报告测试通过/失败

## 第 3 步：4 维度 AI Critique
每个发现标注 🔴致命 / 🟠高 / 🟡中 / 🟢低:

### 3.1 逻辑正确性
边界条件、异常路径、控制流、类型安全。

### 3.2 安全性
SQL 注入、硬编码凭证、路径遍历、日志泄漏。

### 3.3 性能
N+1 查询、DataFrame 逐行循环、DB 索引。

### 3.4 可维护性
函数 >50 行、嵌套 >4 层、命名清晰度、重复代码。

## 第 4 步：输出最终报告

```
============================================================
🔥 代码审计报告
============================================================
审计范围: {文件列表}

📊 总览
- 致命: N  高: N  中: N  低: N

🔴 致命 (必须修复)
- [文件:行号] 问题描述 → 修复建议

🟠 高 (建议修复)
- [文件:行号] 问题描述 → 修复建议

🟡 中 (考虑修复)
- [文件:行号] 问题描述 → 修复建议

🟢 低 (可选优化)
- [文件:行号] 问题描述 → 修复建议

✅ 静态检查: ruff N issues
✅ 测试: N passed / N failed

🔧 修复建议汇总
(可直接应用的代码修复方案)

👀 建议 Driver 重点审查点
1. ...
============================================================
```

规则:
- 每个发现必须带文件路径+行号+修复建议
- 不确定的问题标注「置信度: X%」
- 无问题则写「无致命问题 ✅」
- 输出必须自包含——Driver 不需要回查文件
